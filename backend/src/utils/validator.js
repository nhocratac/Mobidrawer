 const OBJECT_ID_RULE= /^[0-9a-fA-F]{24}$/

 const  OBJECT_ID_RULE_MESSAGE = 'Invalid Object ID'

 const validator = {
        OBJECT_ID_RULE,
        OBJECT_ID_RULE_MESSAGE
 }

 export default validator